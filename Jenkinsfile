pipeline {
    agent any
     stages {
            stage('Fetch Pull Request Data') {
                steps {
                    echo "Trying again!"
                    echo "Received Body Content: ${payload}"
                }
            }
    }

     post {
        always {
            echo 'Always'
        }
      }
}

